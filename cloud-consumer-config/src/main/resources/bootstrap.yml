server:
  port: 8000

spring:
  application:
    name: cloud-consumer-config
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.71.128:8848
      config:
        server-addr: 192.168.71.128:8848
        file-extension: yaml
        namespace: public
        group: DEFAULT_GROUP
