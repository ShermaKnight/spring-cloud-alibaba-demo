server:
  port: 8000

spring:
  application:
    name: cloud-consumer-order
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.71.128:8848

service:
  payment: http://cloud-provider-payment